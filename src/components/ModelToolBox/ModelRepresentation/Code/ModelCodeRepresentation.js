import React from 'react';
import PropTypes from "prop-types";

export const ModelCodeRepresentation = (props) => {
    return (
        <div className={"CodeContainer"}>
            TEST VALUE
        </div>
    )
};

ModelCodeRepresentation.propTypes = {
    code: PropTypes.array
};